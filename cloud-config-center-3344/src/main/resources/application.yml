server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:haoyong001/springcloud-config.git
          search-paths:
            - springcloud-config
      label: main

#注册eureka
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
